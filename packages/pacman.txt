# Official-repo packages this setup relies on.
# Install with: grep -vE '^\s*(#|$)' packages/pacman.txt | xargs -r sudo pacman -S --needed
discord
fish
fzf
git
gnupg
lazygit
openssh
rsync
sshfs
telegram-desktop
uv

# Docker (see apps/docker/install.sh for group/service setup)
docker
docker-compose
docker-buildx
lazydocker

# JetBrains PyCharm Community (see apps/pycharm/install.sh)
pycharm-community-edition

# Display manager (see apps/sddm/install.sh for the caelestia theme + sync setup)
sddm
