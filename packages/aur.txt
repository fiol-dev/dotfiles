# AUR packages this setup relies on (installed via yay).
# Install with: grep -vE '^\s*(#|$)' packages/aur.txt | xargs -r yay -S --needed
# See apps/<name>/install.sh for the notes/caveats behind each one.
amneziavpn-bin
android-studio
caelestia-cli
google-chrome
vscodium-bin
vscodium-bin-marketplace

# caelestia's SDDM login theme, minimalistV2 variant (see apps/sddm/install.sh
# for the sudoers/sync-script/systemctl steps that go with it).
caelestia-sddm-minimalistv2-git
